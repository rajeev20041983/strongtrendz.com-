import React from 'react';

const TestHeader = () => {
  return (
    <header style={{backgroundColor: 'green', color: 'white', padding: '15px'}}>
      Imported Header Component
    </header>
  );
};

export default TestHeader;