/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}",
      "./public/index.html",
    ],
    theme: {
      extend: {
        colors: {
          primary: {
            DEFAULT: '#004a8f', // Main blue color
            dark: '#003366',    // Darker blue for hover states
            light: '#e6f0ff',   // Light blue for backgrounds
          },
          secondary: {
            DEFAULT: '#ff6600', // Orange accent color
            dark: '#e65c00',    // Darker orange for hover states
            light: '#fff0e6',   // Light orange for backgrounds
          }
        },
        fontFamily: {
          sans: ['Inter', 'sans-serif'],
          serif: ['Georgia', 'serif'],
        }
      },
    },
    plugins: [],
  }